apiVersion: batch/v1
kind: Job
metadata:
  name: sleep-job
spec:
  template:
    spec:
      containers:
      - name: sleep-job
        image: busybox
        command: ["sleep", "30"]
      restartPolicy: Never
