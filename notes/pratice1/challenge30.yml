apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx-challenge-30
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-challenge-30
  template:
    metadata:
      labels:
        app: nginx-challenge-30
    spec:
      containers:
        - name: nginx-test
          image: nginx:1.26.3
