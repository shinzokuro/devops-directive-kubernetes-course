apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx-challenge-19
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-challenge-19
  template:
    metadata:
      labels:
        app: nginx-challenge-19
    spec:
      containers:
        - name: nginx-test
          image: nginx:latest
